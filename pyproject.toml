[build-system]
requires = ['hatchling', "hatch-fancy-pypi-readme", "hatch-vcs"]
build-backend = "hatchling.build"

[project]
name = "photomancy"
description = "Divination from light: Bayesian inference and value-of-information for HWO direct imaging"
authors = [{ name = "Corey Spohn", email = "corey.a.spohn@nasa.gov" }]
dynamic = ['readme', 'version']
requires-python = ">=3.11"
license = { file = "LICENSE" }
classifiers = [
    "Development Status :: 2 - Pre-Alpha",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Scientific/Engineering :: Astronomy",
]
dependencies = [
    # >=1.6: the MCLMC tuner requires logdensity_fn from 1.6 on, and 1.5 does
    # not accept the keyword, so the two cannot be supported by one call.
    "blackjax>=1.6",
    "equinox",
    "hwoutils",
    "jax>=0.8.1",
    "jaxlib>=0.8.1",
    "jaxns>=2.6.9",
    "jaxtyping",
    "numpy",
    "numpyro>=0.15.0",
    "optax>=0.2.3",
    # >=1.1.0: KeplerianOrbit.from_period, used by orbit.diagnostics, first
    # ships in 1.1.0; an unpinned orbix resolves to 1.0.0 and fails there.
    "orbix>=1.1.0",
    "skyscapes",
]

[project.optional-dependencies]
dev = ["pre-commit"]
docs = [
    "sphinx",
    "myst-nb",
    "sphinx-book-theme",
    "sphinx-autoapi",
    "sphinxcontrib-mermaid",
    "sphinx_autodoc_typehints",
    "matplotlib",
    "eyepiece>=0.1.0",
]
# photomancy[viz] so the viz tests actually run: without it they all die on the
# eyepiece import and the suite goes green having tested nothing.
test = [
    "nox",
    "pytest",
    "pytest-xdist",
    "hypothesis",
    "pytest-cov",
    "arviz",
    "photomancy[viz]",
]
# Optional extras (jaxns and numpyro are core deps, not extras):
npe = ["flowjax"]       # amortized neural posterior estimation
imaging = ["coronalyze"]  # differentiable detection/extraction likelihood helpers
# eyepiece>=0.1.0 carries every interface photomancy.viz calls (corner,
# corner_overlay, sky_fan, cov_ellipse, save_fig, PlotResult); checked by
# interface in the test suite rather than by version string.
viz = ["eyepiece>=0.1.0"]

[project.urls]
Homepage = "https://github.com/CoreySpohn/photomancy"
Issues = "https://github.com/CoreySpohn/photomancy/issues"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.hooks.vcs]
version-file = "src/photomancy/_version.py"

[tool.hatch.metadata.hooks.fancy-pypi-readme]
content-type = "text/markdown"

[[tool.hatch.metadata.hooks.fancy-pypi-readme.fragments]]
path = "README.md"

[tool.hatch.build.targets.wheel]
packages = ["src/photomancy"]

[tool.hatch.build.targets.sdist]
exclude = ["/scripts", "/docs", "/tests", "/.github"]

[tool.ruff.lint.per-file-ignores]
# Tutorial notebooks: docstring rules and import-position rules are noise in
# cell-structured code.
"docs/**" = ["D", "E402"]

[tool.ruff.lint]
select = ["B", "D", "E", "F", "I", "UP", "RUF"]

[tool.ruff.lint.pydocstyle]
convention = "google"

[tool.pytest.ini_options]
addopts = "-n auto"
